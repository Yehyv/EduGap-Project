const SectionTitle = ({ textTitle }: { textTitle: string }) => {
  return (
    <>
      <div>{textTitle}</div>

      <div className="stretched-line "></div>
    </>
  );
};

export default SectionTitle;
